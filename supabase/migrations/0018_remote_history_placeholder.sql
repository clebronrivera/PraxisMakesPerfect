-- Placeholder: migration version 0018 is recorded on the linked remote database but the
-- original SQL file was not in this repo. Keeps local migration filenames in sync with remote history.
-- If you reset a fresh database from migrations only, verify schema parity with production.
SELECT 1;
